## Get recent tweets by username

1. Add `.env` file with your API key and secret from twitter, BEARER_TOKEN is optional:

```sh
API_KEY=
API_SECRET=
BEARER_TOKEN=
```

2. Run program, pass in username
   - `go run main.go TwitterHandle`
